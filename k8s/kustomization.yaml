apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - hpa.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/name: slopsearx
  app.kubernetes.io/version: "0.1.0"
