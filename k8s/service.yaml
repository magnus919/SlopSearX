apiVersion: v1
kind: Service
metadata:
  name: slopsearx
  labels:
    app: slopsearx
spec:
  selector:
    app: slopsearx
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
