# SlopSearX minimal config
cache:
  ttl_seconds: 300
ranking:
  strategy: presence
routing:
  enabled: true
features:
  ai_dispatch: false
  brave_category_routing: true
  empty_scrape_diagnostics: false
