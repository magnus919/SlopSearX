# EXP-NNN: Short falsifiable hypothesis

## Registration (freeze before candidate work)

- State: registered
- Date, owner, related issue:
- Problem and observed evidence:
- Intended beneficiary and project objective:
- Hypothesis: changing X will improve Y by at least Z under conditions C.
- Baseline SHA and configuration:
- Independent variable / candidate scope:
- Primary metric, units, direction, calculation:
- Minimum practically useful effect and rationale:
- Guardrails and numeric tolerances; immutable contract checks:
- Dataset origin, rights, version/checksum; evaluation unit and query families:
- Development/evaluation split; independent judgments where required:
- Sampling, repetitions, randomization, sample-size rationale:
- Estimator, uncertainty method, multiple-comparison handling (or why inapplicable):
- Fixed stopping rule, time/cost/request budget:
- Exact baseline, candidate, analysis, and regression commands:
- Evidence retention paths:
- Portal impact / design prerequisites:
- Decision rule for supported, not-supported, inconclusive, blocked:
- Registration commit (record after committing; do not amend the frozen plan):

## Readout (append after execution)

- State / outcome:
- Baseline and candidate SHAs; reproduction patch if candidate discarded:
- Environment, dependencies, seeds, fixture checksums:
- Commands actually run and exit statuses:
- Raw evidence links; all trials and exclusions with reasons:
- Baseline value, candidate value, absolute/relative effect, uncertainty:
- Per-family results and guardrail outcomes:
- Deviations from registration and effect on validity:
- Interpretation, limits, and what this does not establish:
- Decision and rationale:
- Implementation PR (supported only):
- Documentation persistence PR/commit (all outcomes):
- Candidate cleanup / rollback:
- Follow-up question and what would justify a retry:
